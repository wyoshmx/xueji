import * as React from 'react'
import styles from './HelloWorld.module.scss'

const HelloWorld = () => {
  return <div className={styles.hello}>Hello World, I am being styled using SCSS Modules!</div>
}

export default HelloWorld
